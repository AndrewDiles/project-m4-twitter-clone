import React from 'react';
import styled, { keyframes } from 'styled-components';

const TweetDetails = ({ children }) => {

  return (
    <div>
      Tweetails
      {children}
    </div>
  )
};

export default TweetDetails;
