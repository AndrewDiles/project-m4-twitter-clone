import React from 'react';
import styled, { keyframes } from 'styled-components';

const HomeFeed = ({ children }) => {

  return (
    <div>
      Home Feed
      {children}
    </div>
  )
};

export default HomeFeed;
